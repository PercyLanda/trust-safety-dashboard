{
  "name": "trust-safety-dashboard",
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "demo": "concurrently -n BACKEND,FRONTEND -c blue,green \"npm run demo:backend\" \"npm run demo:frontend\"",
    "demo:backend": "cd backend && npm run seed && npm run dev",
    "demo:frontend": "cd frontend && npm run dev",
    "dev": "concurrently -n BACKEND,FRONTEND -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "seed": "npm run seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "9.2.1"
  }
}
